// Copyright 2018 Open Trust Group
// Use of this source code is governed by a BSD-style license that can be
// found in the LICENSE file.

library trusty_ipc;

interface TipcPort {
  1: GetInfo() -> (uint32 num_items, uint64 item_size);

  2: Connect(TipcChannel local_handle) -> (status s, TipcChannel? peer_handle);
};
